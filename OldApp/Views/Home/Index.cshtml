@model IEnumerable<OldApp.Models.Target.StudentDTO>
@{ 
    ViewData["Title"] = "Home Page";
    
}
<a asp-action="Add">Nowy</a>
@foreach(var item in Model)
{
<form asp-action="Delete"asp-controller="Home"method="post"asp-route-id="@item.Id">
    @*<p>@item.Id @item.Imie @item.Nazwisko</p><button type="submit">Usuń</button>*@
    <p>@item.Id @item.Name @item.LastName</p><button type="submit">Delete</button>
</form>
}